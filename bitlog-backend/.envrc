# 本地开发用：进入本目录时自动加载 .env（gitignored），离开时自动卸载。
# 用 dotenv 而非 dotenv_if_exists —— .env 缺失时应用会因为 SPRING_PROFILES_ACTIVE
# 未设置而静默退出，所以这里宁可让 direnv 早点报错。
#
# 首次使用：brew install direnv && 在 ~/.zshrc 加 eval "$(direnv hook zsh)" && direnv allow
dotenv
