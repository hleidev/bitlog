<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>top.harrylei.bitlog</groupId>
        <artifactId>bitlog-api</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>bitlog-user-api</artifactId>
    <name>bitlog-user-api</name>
    <description>用户服务 API：DTO/VO/请求参数/Feign 接口，以及跨服务公共响应模型</description>

    <dependencies>
        <!-- ===== 公共基础设施（工具类、上下文） ===== -->
        <dependency>
            <groupId>top.harrylei.bitlog</groupId>
            <artifactId>bitlog-common</artifactId>
        </dependency>

        <!-- ===== Web + 参数校验 ===== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- ===== OpenFeign 客户端接口定义 ===== -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-openfeign</artifactId>
            <!-- api 模块只需要注解；具体 starter 在实现方引入 -->
            <optional>true</optional>
        </dependency>

        <!-- ===== MyBatis-Plus（枚举 @EnumValue 注解） ===== -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ===== Swagger / OpenAPI 文档注解 ===== -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ===== Lombok ===== -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

</project>
